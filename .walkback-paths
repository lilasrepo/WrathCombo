# Pinned 2026-08-15: TC-managed <Version>. Upstream leaves <Version> at 1.0.4.20 across
# releases, so without this pin every refresh resets the TC reserved-revision block
# (1.0.4.2001) back to upstream's static number, the publisher re-clobbers the same tag,
# and installed clients are never offered the update. Moves WrathCombo out of the
# TRACKS_UPSTREAM set into the TC-managed-version set (see plugin_update SKILL.md §5d,
# memory project_lilasrepo_version_pins). Everything else in this csproj is still
# re-established by the mechanical transforms after take-theirs.
WrathCombo/WrathCombo.csproj
